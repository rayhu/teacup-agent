# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# uv / virtualenv
.venv/
.python-version

# Environment variables and secrets (never commit these)
.env
.env.local
mcp.json          # may carry server credentials in "env"; mcp.example.json is the template
agent.yaml        # same reason; agent.example.yaml is the template

# Tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Agent runtime artifacts
memory.json
runs/

# Editors / OS
.vscode/
.idea/
.DS_Store
