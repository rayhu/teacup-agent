# Copy this to .env and fill it in. Offline runs (the default) need no key at all.
# Only `uv run mini-agent --live` uses it.
OPENAI_API_KEY=

GOOGLE_GENAI_USE_ENTERPRISE=FALSE

GOOGLE_API_KEY=

ANTHROPIC_API_KEY=